<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Todo.Views.TodoItemPage"
             Title="{Binding Name}">

    
    <StackLayout Margin="20"
                 VerticalOptions="StartAndExpand">
        <Label Text="Name" />
        <Entry Text="{Binding Name}" />
        <Label Text="dia" />
        <Entry Text="{Binding Dia}" />
        <Label Text="Notes" />
        <Entry Text="{Binding Notes}" />
        <Label Text="Done" />
        <Switch IsToggled="{Binding Done}" />

        <!-- Agregar un botón para cargar imágenes -->
        <Button Text="Upload Image" Clicked="OnUploadImageClicked" />

        <!-- Añadir espacio para mostrar la imagen seleccionada -->
        <Image x:Name="imagePreview" HeightRequest="100" HorizontalOptions="CenterAndExpand" />
        
        <Button Text="Save"
                Clicked="OnSaveClicked" />
        <Button Text="Delete"
                Clicked="OnDeleteClicked" />
        <Button Text="Cancel"
                Clicked="OnCancelClicked" />
    </StackLayout>
</ContentPage>
